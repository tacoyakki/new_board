spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demomo
    username: root
    password: alsl7866!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379